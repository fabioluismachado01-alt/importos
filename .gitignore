# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# Env — NUNCA suba para o repositório
.env
.env.local
.env.*.local

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# Prisma — banco local com seus dados pessoais (NUNCA subir)
prisma/*.db
prisma/*.db-journal
prisma/*.db-shm
prisma/*.db-wal
prisma/prisma/*.db
prisma/prisma/*.db-journal
prisma/prisma/*.db-shm
prisma/prisma/*.db-wal
**/*.db
**/*.db-journal

# Scripts temporários
gerar-relatorio.js
